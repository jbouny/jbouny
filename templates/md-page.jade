extends layout

block prepend title
  | #{ page.title + " - "}
  
block content
  section.row
    div.col-lg-12
      header
        h1= page.title
      - if (page.html.length > 0)
        | !{ typogr(page.html).typogrify() }


